---
created: 2026-04-24T03:31:13.602Z
status: active
---
计划如下，按用户确认的优先级执行：

1. 修复 `ImageMode` 命名冲突
   - 影响文件：[`src/lib/api.ts`](src/lib/api.ts:8)、[`src/app/settings/page.tsx`](src/app/settings/page.tsx:25)
   - 方案对比：
     - 方案 A：继续保留两个同名 `ImageMode`，靠 import 来源区分；改动小，但长期可读性差。
     - 方案 B：将 [`src/lib/api.ts`](src/lib/api.ts:8) 中表示配置/通道的类型重命名为 `ImageProviderMode`；与工作流 `ImageMode` 彻底解耦，语义最清晰。
   - 选用方案 B。
   - 预期修改：重命名类型定义与 [`src/app/settings/page.tsx`](src/app/settings/page.tsx:30) 的相关引用，不影响 [`src/app/image/page.tsx`](src/app/image/page.tsx:54) 中的工作流 `ImageMode`。
   - 潜在副作用：仅类型名变化，不涉及运行时逻辑，风险低。

2. 清理死代码与残留目录
   - 影响文件/目录：[`src/app/accounts/components/account-import-dialog.tsx`](src/app/accounts/components/account-import-dialog.tsx:1)、[`src/server/routes/`](src/server/routes)
   - 已确认：全文搜索未发现 `account-import-dialog` 被引用；[`src/server/routes/`](src/server/routes) 为空目录。
   - 方案：删除未使用组件文件，并移除空目录残留。
   - 潜在副作用：若后续有隐式计划复用该组件，需要重新创建；但当前代码库无引用，清理合理。

3. 拆分图片工作台页面
   - 影响文件：[`src/app/image/page.tsx`](src/app/image/page.tsx:1)，以及新增 `src/app/image/_components/*`
   - 当前判断：[`src/app/image/page.tsx`](src/app/image/page.tsx:1) 体量接近 2000 行，状态、事件与视图高度耦合，是当前主要架构负担。
   - 推荐拆分方式：
     1) 抽离纯展示常量/小工具（如历史列表项、灵感卡片、会话 turn 渲染）
     2) 保留页面中的核心状态与提交逻辑，先不跨文件拆业务副作用函数，降低回归风险
     3) 将 JSX 大块拆为以下子组件：
        - `history-sidebar.tsx`
        - `empty-state.tsx`
        - `conversation-turn.tsx`
        - `composer-panel.tsx`
   - 方案对比：
     - 方案 A：一次性把状态与逻辑也抽成 hooks/store，结构最理想，但改动面大、回归风险高。
     - 方案 B：先做“视图分层”，页面仍保留状态和核心 handler，只把大型 JSX 子树拆出去；收益明显且最稳。
   - 选用方案 B。
   - 潜在副作用：组件 props 会较多，但这是可控的过渡阶段，优先保证行为不变。

4. 执行顺序
   1) 重命名 `ImageMode` -> `ImageProviderMode`
   2) 删除死代码与空目录
   3) 新建 `src/app/image/_components/` 并拆分 [`src/app/image/page.tsx`](src/app/image/page.tsx:1) 的大型 JSX
   4) 最后跑构建验证

如果你批准这个计划，我将退出计划模式并开始实施上述改动。